<!DOCTYPE html>
<html class="antialiased">
  <%= render "shared/head" %>
  <body class="h-full font-sans font-normal leading-normal text-gray-800 bg-gray-200">
    <%= render "admin/backend/flashes" -%>
    <div class="flex h-screen">
      <div class="flex flex-col flex-1">
        <main class="flex flex-col items-center flex-grow px-4 py-2 overflow-scroll">
          <%= content_for?(:content) ? yield(:content) : yield %>
        </main>
        <%= render "shared/footer" %>
      </div>
    </div>
  </body>
</html>