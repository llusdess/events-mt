<!DOCTYPE html>
<html class="antialiased">
  <%= render "shared/head" %>
  <body class="h-full font-sans font-normal leading-normal text-gray-800 bg-gray-100">
    <div class="flex h-screen">
      <div class="flex flex-col flex-1">
        <main class="flex-grow overflow-scroll border-t prose">
          <%= content_for?(:content) ? yield(:content) : yield %>
        </main>
        <%= render "shared/footer" %>
      </div>
    </div>
  </body>
</html>
